{
  "name": "decentralized-voting-system",
  "version": "1.0.0",
  "description": "Tamper-proof on-chain voting system",
  "main": "deploy.js",
  "scripts": {
    "test": "npx hardhat test",
    "deploy": "npx hardhat run deploy.js",
    "compile": "npx hardhat compile"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^3.0.0",
    "hardhat": "^2.28.6"
  }
}
